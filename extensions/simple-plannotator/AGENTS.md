# simple-plannotator

浏览器审查 git 改动与标注 Markdown。

## 目录结构

`index.ts`: 扩展入口
`tests/`: 测试
`plannotator-browser.d.ts`: 浏览器侧类型声明
`package.json` / `README.md` / `CHANGELOG.md` / `LICENSE`: 包元数据与说明

## 开发注意事项

- fork 只改本仓需要的兼容与行为，不重写上游通用说明

## 基本约束

（暂无已确认条目。新增须用户确认后再写入。）
