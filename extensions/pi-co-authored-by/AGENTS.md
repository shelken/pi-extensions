# pi-co-authored-by

agent 执行 `git commit` 时自动写入 Co-Authored-By / Generated-By trailer。

## 目录结构

`index.ts`: 扩展入口
`lib/`: hook / commit 实现与测试
`package.json` / `README.md` / `CHANGELOG.md` / `LICENSE`: 包元数据与说明

## 基本约束

（暂无已确认条目。新增须用户确认后再写入。）
