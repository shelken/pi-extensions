# pi-add-dir

把会话外目录加入当前 session，并注入上下文与 skills。

## 目录结构

`index.ts`: 扩展入口
`helpers.ts`: 与入口拆开的纯逻辑
`tests/`: 测试
`package.json` / `README.md` / `CHANGELOG.md` / `LICENSE`: 包元数据与说明

## 开发注意事项

- 本仓已砍 monorepo 推荐/suggestions 等能力；无明确需求不要加回

## 基本约束

（暂无已确认条目。新增须用户确认后再写入。）
