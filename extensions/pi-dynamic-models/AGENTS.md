# pi-dynamic-models

从 provider 发现远端模型并用 models.dev 补全参数，注册为 `(AUTO)` 模型。

## 目录结构

`index.ts`: 扩展入口
`src/`: 发现、缓存、配置与请求实现
`tests/`: 测试
`package.json` / `README.md` / `CHANGELOG.md` / `LICENSE`: 包元数据与说明

## 基本约束

（暂无已确认条目。新增须用户确认后再写入。）
